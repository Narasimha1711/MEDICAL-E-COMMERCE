name: Backend CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'Backend/**'
  pull_request:
    branches: [main]
    paths:
      - 'Backend/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Cache MongoDB binaries
        uses: actions/cache@v4
        with:
          path: ~/.cache/mongodb-memory-server
          key: ${{ runner.os }}-mongodb-memory-server-${{ hashFiles('Backend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-mongodb-memory-server-
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: Backend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('Backend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Install dependencies
        run: npm install
        working-directory: ./Backend
      - name: Run tests
        run: npm test
        working-directory: ./Backend
        env:
          NODE_ENV: test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: Backend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('Backend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Install dependencies
        run: npm install
        working-directory: ./Backend
      - name: Debug directory structure
        run: |
          pwd
          ls -R
        working-directory: ./Backend
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Deploy to Vercel
        run: vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: ./Backend
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}


# name: Backend CI

# on:
#   push:
#     branches: [main]
#     paths:
#       - 'Backend/**'
#   pull_request:
#     branches: [main]
#     paths:
#       - 'Backend/**'

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     steps:
#       # Step 1: Checkout the code
#       - uses: actions/checkout@v4

#       # Step 2: Set up Node.js
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'

#       # Step 3: Cache MongoDB binaries
#       - name: Cache MongoDB binaries
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/mongodb-memory-server
#           key: ${{ runner.os }}-mongodb-memory-server-${{ hashFiles('Backend/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-mongodb-memory-server-

#       # Step 4: Cache node_modules
#       - name: Cache node modules
#         uses: actions/cache@v4
#         with:
#           path: Backend/node_modules
#           key: ${{ runner.os }}-node-${{ hashFiles('Backend/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-node-

#       # Step 5: Install dependencies
#       - name: Install dependencies
#         run: npm install
#         working-directory: ./Backend

#       # Step 6: Run tests
#       - name: Run tests
#         run: npm test
#         working-directory: ./Backend
#         env:
#           MONGODB_URI: ${{ secrets.MONGODB_URI }}
#           JWT_SECRET: ${{ secrets.JWT_SECRET }}
#           RAZORPAY_KEY: ${{ secrets.RAZORPAY_KEY }}
#           RAZORPAY_SECRET: ${{ secrets.RAZORPAY_SECRET }}